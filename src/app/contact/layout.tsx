import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Contact & Campus Directions — His Kids High School, Kayunga",
  description: "Get in touch with His Kids High School in Kayunga District, Uganda. Reach the admissions office, headteacher, bursar, or find travel directions."
};

export default function ContactLayout({ children }: { children: React.ReactNode }) {
  return children;
}
